export interface DashboardData {
    id: string;
    title: string;
    value: number;
    timestamp: Date;
}

export interface User {
    id: string;
    name: string;
    email: string;
    role: string;
}